node_modules
dist
.deploy
audio
ayoub

# Timing-pipeline scratch audio (large MP3s; masters live in GCS)
_timing_analysis/

# macOS folder metadata
.DS_Store

# compare_generator regeneration artifacts (canonical = comparisons.sqlite; the
# fixed copy + backups live in GCS, not git)
compare_generator/comparisons.fixed.*
compare_generator/backups/
compare_generator/backups_LATEST.txt
compare_generator/needs_review.json
compare_generator/run_summary.json
compare_generator/*.sqlite-shm
compare_generator/*.sqlite-wal
models/

# tasmee dev: ort wasm + model served locally (model ships via on-device download)
public/ort/
public/models/

# tasmee golden clips are voice recordings — keep out of the PUBLIC repo
# (project convention: audio masters live in GCS, not git). Truth JSONs stay.
tests/**/*.wav

# Layer 2 (muaalem) python env — see scripts/tasmee-deep-check.py
.venv-muaalem/
